/**
 * KPI domain holds targets and qualitative thresholds consumed by the KPI calculator service.
 * @typedef {Object} KpiTarget
 * @property {string} id
 * @property {string} label
 * @property {number} targetPercent — e.g. on-time delivery target
 */
